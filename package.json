{
  "name": "pix-esteira",
  "version": "1.0.0",
  "private": true,
  "description": "Backend Vercel para monitorar Pix recebidos no Mercado Pago",
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "test": "node --test",
    "check": "node --check api/health.js && node --check api/pix/list.js && node --check api/pix/sync.js && node --check api/pix/webhook.js && node --check api/pix/ingest.js"
  },
  "dependencies": {
    "firebase-admin": "^13.5.0"
  }
}
